# TestPostApp

## Running the App

Clone the repository and run npm install.

Run ng serve for a dev server. Navigate to http://localhost:9001/. The app will automatically reload if you change any of the source files.

You can also run ng serve -o to open the app directly in your browser. It will automatically navigate to http://localhost:9001/
